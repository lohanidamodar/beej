include: package:lints/recommended.yaml

analyzer:
  exclude:
    - templates/**
  errors:
    # Templates are data; the generator is the only thing that reads them.
    unused_element: warning

linter:
  rules:
    - prefer_single_quotes
    - directives_ordering
    - cancel_subscriptions
    - close_sinks
